# Project Structure

This project is developed using the model-view-controller (MVC) architecture.
The `/lib` directory contains:

* `models/`: Contains the data models and business logic of the application.
* `views/`: Contains the user interface components and templates.
* `controllers/`: Contains the controllers that handle user input and interact with the models and views.